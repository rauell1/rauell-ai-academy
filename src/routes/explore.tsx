import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowRight, Compass, GraduationCap, Wrench } from "lucide-react";
import { CourseCard, PageIntro } from "@/components/Cards";
import { courses } from "@/data/academy";
export const Route=createFileRoute("/explore")({component:Explore});
function Explore(){return <><PageIntro eyebrow="Explore the Academy" title="Find the right place to begin." copy="Tell us where you are going, or start with a recommended course. There is no wrong starting point."/><section className="mx-auto max-w-7xl px-5 py-16 lg:px-8"><div className="grid gap-5 md:grid-cols-3">{[[Compass,"I am new to AI","Build confidence with a clear, practical introduction.","Foundations"],[GraduationCap,"I want to use AI better","Improve your prompting, research, and verification habits.","Core skills"],[Wrench,"I want to build with AI","Create applications, automations, and reliable agents.","Builder track"]].map(([Icon,t,c,k])=>{const I=Icon as typeof Compass;return <Link key={t as string} to="/courses" className="card card-lift p-6"><I className="h-7 w-7 text-leaf"/><p className="eyebrow mt-8 text-ink/40">{k as string}</p><h2 className="font-display mt-3 text-2xl font-bold">{t as string}</h2><p className="mt-3 text-sm leading-6 text-ink/60">{c as string}</p><ArrowRight className="mt-7 h-5 w-5"/></Link>})}</div><h2 className="font-display mt-20 text-3xl font-bold">Recommended starting points</h2><div className="mt-8 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{courses.slice(0,3).map(c=><CourseCard key={c.slug} course={c}/>)}</div></section></>}
