import { createFileRoute } from "@tanstack/react-router";
import { ArrowRight, Clock3, LockKeyhole } from "lucide-react";
import { PageIntro } from "@/components/Cards";
import { labs } from "@/data/academy";
export const Route=createFileRoute("/labs")({component:Labs});
function Labs(){return <><PageIntro eyebrow="Practical labs" title="Learn by testing, making, and reflecting." copy="Guided sandboxes turn ideas into practical skills. Each lab gives you a challenge, instructions, hints, a scoring rubric, and a reflection."/><section className="mx-auto grid max-w-7xl gap-5 px-5 py-16 md:grid-cols-2 lg:px-8">{labs.map(l=>{const I=l.icon;return <article key={l.title} className="card card-lift overflow-hidden"><div className={`${l.color} flex h-48 items-center justify-center`}><div className="grid h-24 w-24 place-items-center rounded-[1.5rem] bg-ink text-white shadow-xl"><I className="h-11 w-11"/></div></div><div className="p-6"><div className="flex items-center gap-3 text-[11px] font-bold uppercase tracking-wider text-ink/45"><span>{l.level}</span><span className="inline-flex items-center gap-1"><Clock3 className="h-3.5 w-3.5"/>{l.time}</span></div><h2 className="font-display mt-3 text-2xl font-bold">{l.title}</h2><p className="mt-2 text-sm leading-6 text-ink/62">{l.copy}</p><button disabled={l.status!=="Ready"} className="mt-6 inline-flex items-center gap-2 rounded-full bg-ink px-5 py-3 text-sm font-bold text-white disabled:bg-ink/15 disabled:text-ink/45">{l.status==="Ready"?<>Open lab <ArrowRight className="h-4 w-4"/></>:<><LockKeyhole className="h-4 w-4"/>Coming soon</>}</button></div></article>})}</section></>}
